# Environment and credentials - NEVER commit these
.env
*.env
.env.*
!.env.example

# Common credential file patterns
*_token*
*credentials*
*secret*

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Flow outputs and backups (may contain project data - never commit)
backups/
outputs/

# User-specific import config (contains column names - gitignore the live copy)
config/import_config.yaml

# OS
.DS_Store
Thumbs.db
